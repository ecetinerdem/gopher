

services:
  db:
    image: postgres:16.10
    container_name: postgres_db
    environment:
      POSTGRES_DB: gopher
      POSTGRES_USER: pst
      POSTGRES_PASSWORD: pst
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"


volumes:
  db-data:
